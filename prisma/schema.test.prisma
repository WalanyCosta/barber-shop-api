generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

model account {
    id          String @id @default(uuid())
    name        String
    email       String
    password    String
    phone       String
    accessToken String
}


